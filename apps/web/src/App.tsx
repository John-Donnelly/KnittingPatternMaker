import { CORE_VERSION } from 'knitting-pattern-core';

export function App() {
  return (
    <main>
      <h1>Knitting Pattern Maker</h1>
      <p>Scaffold ready (core v{CORE_VERSION}). Upload flow coming next.</p>
    </main>
  );
}
