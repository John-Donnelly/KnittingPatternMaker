{
  "name": "knitting-pattern-core",
  "version": "0.1.0",
  "description": "Deterministic image-to-pixel-art and knitting pattern generation algorithms.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
