{
  "name": "knitting-pattern-maker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "UNLICENSED",
  "description": "Convert any image into deterministic pixel art and a complete, printable knitting pattern.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run build -w packages/core && npm run test -w packages/core && npm run test -w apps/api && npm run test -w apps/web",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run typecheck -w packages/core && npm run build -w packages/core && npm run typecheck -w apps/api && npm run typecheck -w apps/web && npm run typecheck -w apps/worker",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:worker": "npm run dev --workspace=apps/worker",
    "deploy:worker": "npm run deploy --workspace=apps/worker"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.1"
  }
}
